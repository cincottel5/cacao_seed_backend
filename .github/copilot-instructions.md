# Copilot Instructions

## Current repository state

This repository is an empty backend scaffold. At the time this file was created, it contains no application source, dependency manifest, build configuration, test suite, lint configuration, README, contribution guide, or other AI-assistant instructions.

## Commands

No build, test, lint, formatter, or single-test commands are configured yet. When tooling is added, document the exact full-suite and single-test commands here.

## Architecture and conventions

No application architecture or repository-specific implementation conventions have been established yet. Derive new guidance from the first committed backend structure and configuration rather than assuming a language, framework, package manager, or test runner.
